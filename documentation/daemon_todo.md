# Daemon — TODO для SQUEEZE_ZZ_ATR

## 1. ~~OCO grouping: заменить совпадение по `signal_data` на поле `oco_group`~~ — ОТМЕНЕНО

**Решение принято:** OCO реализуется на стороне стратегии, а не демона.

- Стратегия держит флаг `active_oco` и сама перестаёт мониторить второй уровень после срабатывания первого
- Вход — маркет на открытии бара после сигнала (как уже работает для других стратегий)
- На MT5 никаких pending-ордеров не размещается
- Если демон упадёт/разрыв связи — на MT5 чисто, никаких висящих ордеров

Поля `oco_group` в протоколе и БД **не добавлять**.  
`GetOcoPendingTickets()` для SQUEEZE_ZZ_ATR **не используется**.

---

## 2. RCalc: различать `sl_dist` по направлению позиции

**Статус: не требует правок** — каждый сигнал несёт свой `sl_dist`, Reconciler получает правильное значение автоматически.

---

## 3. Верификация: копируется ли `signal_data` из pending в position при исполнении

**Где проверить:**  
`PendingOrderManager.cs` → блок заполнения позиции при `FILLED`:
```csharp
SignalData = rec.SignalData,   // ← должна быть эта строка
```
Если её нет — R-расчёт вернёт 0.

**Статус:** судя по коду (~строка 253) поле присваивается. Верифицировать на первой live сделке через dashboard → Positions → signal_data.

---

## Приоритет

| # | Задача | Статус |
|---|--------|--------|
| 1 | OCO group | ~~Не реализовывать~~ — закрыто решением |
| 3 | Верификация signal_data в position | Проверить на первой live сделке |
| 2 | RCalc sl_dist | Не требует правок |
